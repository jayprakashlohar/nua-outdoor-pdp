# `src/router`

**Assignment:** Optional folder — use if you add routing.

## Recommended for this project

- **React Router** (or similar) to reflect **selected colour + size in the URL** so the PDP is shareable/deep-linkable.
- Route example: `/product/:id` with query params `?color=...&size=...` (your naming choice — document in `DECISIONS.md`).

## Suggested files

- `index.tsx` or `routes.tsx` — route definitions
- Wire provider in `main.tsx` above the app tree

If you use a single-page PDP with no extra routes, still document URL sync strategy here or in `DECISIONS.md`.
