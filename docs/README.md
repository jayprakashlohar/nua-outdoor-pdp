# `docs`

**Assignment:** Store your **Lighthouse** report screenshot here (performance evaluation ~10%).

## What to add

1. Run Lighthouse on the PDP (mobile + desktop if possible)
2. Save screenshot(s) e.g. `lighthouse-desktop.png`, `lighthouse-mobile.png`
3. Note any fixes you applied in root `README.md` or `DECISIONS.md`

Optional: short `PERFORMANCE.md` listing scores and changes made.
