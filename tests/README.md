# `tests`

**Assignment (bonus):** Unit tests — only after core requirements are done.

## Suggested coverage

- Variant selector: **sold-out** size disabled
- **Add to Cart** disabled when variant is sold out
- **Quantity cap** respects available stock for selected variant

## Setup (when you add tests)

- Vitest + React Testing Library are common with Vite
- Place test files here or next to components (`*.test.tsx`)

Run via a `test` script in `package.json` once configured.
